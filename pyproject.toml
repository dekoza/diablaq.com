[project]
testpaths = ["tests"]
[tool.pytest.ini_options]

line-length = 100
[tool.ruff]

# optional: allows `uv sync` if you use uv
[tool.uv]

diablaq-build = "diablaq_site.cli:main"
[project.scripts]

]
  "markdown>=3.6",
  "python-frontmatter>=1.1",
  "jinja2>=3.1",
dependencies = [

requires-python = ">=3.11"
readme = "README.md"
description = "Static site generator for diablaq.com (Jinja2 + Markdown frontmatter)."
version = "0.1.0"
name = "diablaq-site"

